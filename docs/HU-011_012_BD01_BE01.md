# HU-011 BD-01, BE-01 + HU-012 BD-01: Membresía de equipos con rol interno

## Contexto

- Django 4.2 + DRF + PostgreSQL (Supabase). Apps en `apps/`.
- El modelo `Equipo` ya existe en `apps/equipos/models.py` (completado en HU-010).
- El modelo `Usuario` ya existe en `apps/usuarios/models.py`. **Leer ese archivo** para confirmar el nombre exacto de la clase y el app_label antes de escribir la FK.
- El modelo `Equipo` tiene FK a `Proyecto`. La validación de "un estudiante no puede estar en dos equipos del mismo proyecto" requiere acceder a `equipo.proyecto`. **Leer `apps/equipos/models.py`** para confirmar el campo.
- **No modificar ningún modelo ni migración existente.**

---

## Paso 1 — Leer archivos de dependencia antes de escribir nada

```
Read apps/usuarios/models.py
Read apps/equipos/models.py
Read apps/equipos/migrations/  (listar para saber el número de la próxima migración)
```

---

## Paso 2 — HU-011 BD-01 + HU-012 BD-01: Crear `apps/equipos/models.py` (agregar `MiembroEquipo`)

> **Editar** el archivo existente — no reescribirlo completo. Agregar la clase `MiembroEquipo` debajo de `Equipo`.

Campos de `MiembroEquipo`:

| Campo | Definición Django |
|---|---|
| `id` | AutoField PK (implícito) |
| `equipo` | `ForeignKey('equipos.Equipo', on_delete=models.CASCADE, related_name='miembros')` |
| `usuario` | `ForeignKey('usuarios.Usuario', on_delete=models.CASCADE, related_name='membresías')` — ajustar app_label según lo leído en Paso 1 |
| `fecha_asignacion` | `DateTimeField(auto_now_add=True)` |
| `estado` | `CharField(max_length=10, choices=[('activo','Activo'),('retirado','Retirado')], default='activo')` |
| `rol_interno` | `CharField(max_length=20, choices=[('lider','Líder'),('desarrollador','Desarrollador'),('disenador','Diseñador'),('tester','Tester'),('analista','Analista')], blank=True, default='')` |
| `descripcion_responsabilidades` | `TextField(blank=True, default='')` |

**`class Meta`:**

```python
class Meta:
    constraints = [
        models.UniqueConstraint(
            fields=['equipo', 'usuario'],
            name='unique_usuario_por_equipo'
        )
    ]
```

`__str__` retorna `f"{self.usuario} en {self.equipo}"`.

---

## Paso 3 — HU-011 BE-01: Crear `apps/equipos/serializers.py` (agregar `MiembroEquipoSerializer`)

> **Editar** el archivo existente — agregar debajo de `EquipoSerializer`.

Clase `MiembroEquipoSerializer(serializers.ModelSerializer)`:

- `Meta.model = MiembroEquipo`, `Meta.fields = '__all__'`
- Campo `equipo` como `read_only=True` (se inyecta desde la vista).
- Método `validate(self, data)` con **dos validaciones de negocio**:

**Validación 1 — estudiante ya pertenece a otro equipo del mismo proyecto:**

```python
equipo = self.context['equipo']  # inyectado por la vista
usuario = data['usuario']
proyecto = equipo.proyecto

ya_en_proyecto = MiembroEquipo.objects.filter(
    usuario=usuario,
    estado='activo',
    equipo__proyecto=proyecto
).exclude(equipo=equipo).exists()

if ya_en_proyecto:
    raise serializers.ValidationError(
        "El estudiante ya pertenece a otro equipo en este proyecto."
    )
```

**Validación 2 — equipo no puede exceder su cupo máximo:**

```python
miembros_activos = MiembroEquipo.objects.filter(
    equipo=equipo,
    estado='activo'
).count()

if miembros_activos >= equipo.cupo_maximo:
    raise serializers.ValidationError(
        f"El equipo ha alcanzado su cupo máximo de {equipo.cupo_maximo} integrantes."
    )
```

- Retornar `data` al final de `validate`.

> **Nota:** El contexto `{'equipo': equipo_instance}` debe ser pasado por la vista al instanciar el serializador: `MiembroEquipoSerializer(data=request.data, context={'equipo': equipo})`. Agregar un comentario en el serializador indicando esto.

---

## Paso 4 — Generar y aplicar migración

```bash
python manage.py makemigrations equipos
python manage.py migrate
```

Confirmar que la migración crea la tabla `equipos_miembroequipo` con el constraint `unique_usuario_por_equipo` y los campos `rol_interno` y `descripcion_responsabilidades`.

---

## Restricciones

- No crear vistas ni urls en esta tarea.
- No tocar archivos fuera de `apps/equipos/`.
- No reescribir `models.py` ni `serializers.py` completos — usar edición parcial (agregar al final del archivo).
- Los campos `rol_interno` y `descripcion_responsabilidades` deben estar en el mismo modelo desde el inicio: no crear una migración separada para HU-012, ya que ambas subtareas afectan la misma tabla y se implementan juntas.

---

## Entregables esperados

1. `apps/equipos/models.py` — clase `MiembroEquipo` agregada con `UniqueConstraint`, `rol_interno` y `descripcion_responsabilidades`.
2. `apps/equipos/serializers.py` — `MiembroEquipoSerializer` con ambas validaciones de negocio.
3. Migración generada aplicada sin errores contra Supabase.
